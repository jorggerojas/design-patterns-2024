// Define the variable with the floating-point value
let roundUp = 1.5;

// Round the number up using Math.round()
let rounded = Math.round(roundUp);

// Print the rounded number to the console
console.log(rounded); 