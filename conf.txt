cd carpeta_name
npm init -y
agragar comandos a scripts
"scejemplo": "ts-node ruta/ruta.ts" 
npm run scejemplo

npx ts-node ruta/ruta.ts

git clone https://github.com/gaelv01/caravan-bus-management     //clonar repo

git checkout GaelPosada01       //acceder auna rama
git add .    //encapsular todos los archivos y carpetas
git commit -m "Reorganizacion de archivos"    //descripcion de la pullrequest
git push origin GaelPosada01     //subir los archivos a la rama